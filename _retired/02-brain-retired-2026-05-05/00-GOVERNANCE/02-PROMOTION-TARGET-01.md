# 02-PROMOTION-TARGET-01

## 最终目标
优化后的新 02，在完成验证与收口后，
应正式覆盖：

`~/System-Snapshots/02-brain`

覆盖完成后，`System-Snapshots/02-brain` 应重新成为唯一有效的 02 脑母本真源。

## 当前原则
1. 当前不是否定 Core 继续读取 `~/System-Snapshots/02-brain`
2. 真正要解决的是：当前 `System-Snapshots/02-brain` 里仍是旧 02
3. 因此最终正确动作，不是让 Core 改去读施工区
4. 而是让优化后的新 02 正式扶正，覆盖旧 `System-Snapshots/02-brain`

## 当前阶段判断
这一步现在还不能立刻执行。
原因不是方向不对，
而是：
- 新 02 仍处于验证后收口阶段
- PG 为接近通过
- Core 为基本通过
- Core 对旧 02 的引用习惯仍未完全脱钩

## 正确顺序
1. 继续完成 Core 脱旧收口
2. 继续完成新 02 收口
3. 确认新 02 已具备扶正条件
4. 正式覆盖 `~/System-Snapshots/02-brain`
5. 再清退 `SYOO1-02-Brain-Rebuild` 等施工区

## 一句话收口
最终不是让 Core 永久绕开 `System-Snapshots/02-brain`，
而是：
**让新的、优化后的 02 正式进入 `System-Snapshots/02-brain`，重新成为唯一真源。**
