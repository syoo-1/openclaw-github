# 规则入口提示

本目录下的规则优先以 `rules.yaml` 为机器可读入口。
旧长规则文件只作为来源材料与历史依据，不再继续手工扩写。
新增规则应先压缩为 `rules.yaml` 最小字段条目。

`99-DEFERRED/` 只作为延期归档来源堆，默认不进入当前有效规则候选。

`08-IMPORT-STAGING/` 是来源暂存区，不整体视为当前规则候选；只有被 `rules.yaml` 的 `source` 明确引用的文件，才作为当前规则来源。
