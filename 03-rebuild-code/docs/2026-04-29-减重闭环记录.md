# 2026-04-29 减重闭环记录

## 一、本轮定位

本轮不是大规模删文件，而是一次“关键口径校准 + 规则入口归一 + 局部卡压缩 + PG/Core 验收闭环”。

目标：

1. 修正旧现实残留。
2. 防止 03 工艺脚本继续生成旧端口。
3. 建立规则权威入口表。
4. 压缩局部工具卡中的重复总纲说明。
5. 用 PG / Core 固定验收题确认减重后未破坏主脑读取与协同口径。

---

## 二、冻结点

减重前已打标签：

- `before-trim-2026-04-29`

对应提交：

- `989f3c2 补03：固化Core回传PG后台桥工艺`

用途：

- 作为本轮减重前回滚点。
- 后续如发现减重误伤，可回到此标签核对。

---

## 三、已完成修正

### 1. 当前现实口径修正

已将当前有效口径统一为：

- Core 现役端口：`12289`
- PG 现役端口：`58789`
- `60789`：旧测试口，不作为正式依据

对应提交：

- `a03b2a3 减重：修正现役端口口径为Core 12289`

### 2. 03 工艺脚本端口修正

已将 03 生成 / 启动 Core 的默认端口统一为：

- `12289`

对应提交：

- `06e20ec 减重：修正03工艺脚本Core默认端口为12289`

### 3. 旧 OpenClaw 固定版本提示移除

已将旧固定提示：

- `Confirm openclaw is 2026.2.26`

改为动态检查：

- `openclaw --version`

对应提交：

- `a3db487 减重：移除03脚本旧OpenClaw固定版本提示`

### 4. 规则权威入口表建立

新增：

- `03-rebuild-code/docs/00-规则权威入口表.md`

作用：

- 明确不同问题优先读取哪张权威卡。
- 防止局部卡重复解释上位规则。
- 作为后续规则减重的判断依据。

对应提交：

- `c17ab07 减重：新增03规则权威入口表`

### 5. 局部工具卡压缩

已压缩：

- `03-PG-Gemma4外部能力工具-V1.md`
- `03-Core-oMLX外部工具接入卡-V1.md`

原则：

- 保留上位卡引用。
- 保留局部用途、调用入口、边界、硬结论。
- 压缩重复总纲说明。
- 不动 03-Core-PG 上位总卡。
- 不动 02 长期原则。
- 不动 PG 身份层。

对应提交：

- `8cc971e 减重：压缩PG Gemma4外部工具局部卡`
- `dfaaa68 减重：压缩Core oMLX外部工具局部卡`

---

## 四、验收结果

### 1. 安全体检

`safety-check-v1.sh` 输出：

- `FAIL=0`
- `WARN=0`
- 结论：通过，当前系统处于绿色状态

已确认：

- Core 12289 正在监听
- PG 58789 正在监听
- LaunchAgent 全部加载
- 搜索体系存在并通过冒烟测试
- Git 工作区干净

### 2. PG 固定验收

PG 已通过减重后固定验收题 V2。

关键命中：

- 近一身份正确
- 老林身份正确
- Core = `12289`
- PG = `58789`
- `60789` = 旧测试口，不作为正式依据
- Core / 02 / GitHub-02 = 同脑三位
- PG 与 Core 并列，不从属
- 前层降权，后层为准
- Gemma4 / oMLX 是外部工具，不是主脑
- 协同完整项目记忆回流 Core
- 能列出正式入口依据

### 3. Core 固定验收

Core 已通过减重后固定验收题 V2。

关键命中：

- 小一身份正确
- 老林身份正确
- Core = `12289`
- PG = `58789`
- `60789` = 旧测试口，不作为正式依据
- Core / 02 / GitHub-02 = 同脑三位
- PG 与 Core 并列独立
- 前层降权，后层为准
- 03 = 通用迁移 / 拼装 / 验收工艺母本
- Gemma4 / oMLX = 外部能力工具，不是主脑
- 协同完整项目记忆回流 Core
- 能列出正式入口依据
- 安全体检绿色需以 `safety-check-v1.sh` 输出为准

---

## 五、重要边界

本轮没有做：

1. 没有大规模删除 02。
2. 没有大规模删除 PG 脑。
3. 没有拆 03-Core-PG 上位总卡。
4. 没有改身份层。
5. 没有改长期原则层。
6. 没有把局部卡压成空壳。
7. 没有把“安全体检绿色”冒充为“系统完全达标”。

本轮完成的是：

- 口径校准
- 入口归一
- 局部卡压缩样板
- PG / Core 验收闭环

---

## 六、当前结论

本轮减重闭环通过。

当前可说：

- 安全体检绿色
- PG / Core 固定验收通过
- Core / PG 当前端口口径已统一
- 规则读取入口已归一
- 局部工具卡压缩方法已验证

不可夸大为：

- 全系统所有历史残留已清空
- 所有规则都已压缩完成
- 02 / PG 脑已经系统性精简完成
- 系统完全产品级稳定

---

## 七、后续建议

下一轮如继续减重，建议只处理：

- `PG/brain/00-GOVERNANCE/CONTINUITY/全系统-Gemma4共享占用规则-V1.md`

处理原则：

1. 单独一轮。
2. 先读，不直接改。
3. 只压缩重复总纲。
4. 不破坏 PG 连续性规则。
5. 压缩后必须再次跑 PG / Core 固定验收题。

