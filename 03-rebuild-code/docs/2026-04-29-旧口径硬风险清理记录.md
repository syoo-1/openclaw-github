# 2026-04-29 旧口径硬风险清理记录

## 一、本轮定位

本轮不是大规模压缩，而是只读扫描 02 / PG 正式区中的旧口径风险，并只处理明确硬风险点。

扫描范围包括：

- 旧端口口径：`38789` / `60789`
- 旧模型口径：qwen / qwen2.5 / ollama/qwen
- 旧搜索口径：brain-search.sh / brain-search-v3 / router-search-v1 / memory-search-v1
- 07 / 08 / 99 是否被误当正式入口
- 高重复规则卡候选

---

## 二、扫描结论

### 1. 旧端口口径

`60789` 当前均为正确说明：

- 旧测试口
- 不属于现役实例
- 不作为正式依据

`38789` 发现 1 个硬风险点：

- `02-brain/04-COMPILED/RULES/CORE-VALIDATION-RESULT-01.md`

问题：

- 旧文本曾写“38789 对应现役 Core”
- 容易反向污染当前 Core 端口现实

已处理。

---

### 2. 旧模型口径

qwen / qwen2.5 相关表述当前均为正确说明：

- qwen 退出 PG 主链
- qwen 属历史旧口径
- 临时切换不得写回默认模型

本轮不处理。

---

### 3. 旧搜索口径

当前搜索链已校准为：

- `router-search-v1.sh`
- `brain-search-v3.sh`
- `memory-search-v1.sh`

旧 `brain-search.sh` 只作为历史兼容或旧工具说明，没有继续冒充正式底盘。

本轮不处理。

---

### 4. 07 / 08 / 99 入口风险

扫描结果显示：

- `07-RETIRING`
- `08-IMPORT-STAGING`
- `99-DEFERRED`

当前均主要被定义为非正式入口、暂存区、退役区或延迟区。

本轮不处理。

---

## 三、本轮实际修改

文件：

- `02-brain/04-COMPILED/RULES/CORE-VALIDATION-RESULT-01.md`

处理结果：

- 增加“当前状态说明”。
- 明确本卡是历史验证结果，不是当前 Core 运行现实入口。
- 明确当前正式口径：
  - Core = `12289`
  - PG = `58789`
  - `60789` = 旧测试口，不作为正式依据
- 将“Core 现役对话链（38789 对应现役 Core）”改为历史验证来源说明。
- 明确不得从本卡反向定义当前 Core 现役端口。

提交：

- `98509d7 减重：降权Core旧验证结果端口口径`

---

## 四、复查结果

复查 `38789` 后，仅剩两类说明：

1. 历史 Core 验证对话链中曾使用旧口径。
2. 退役区中曾出现旧 Core 检查端口。

均不再作为当前 Core 端口定义。

---

## 五、验收结果

执行：

- `03-rebuild-code/tools/safety-check-v1.sh`

结果：

- Core `12289` 正在监听
- PG `58789` 正在监听
- LaunchAgent 全部加载
- 搜索体系检查通过
- 搜索路由冒烟测试通过
- System-Snapshots 当前分支为 `main`
- 工作区干净
- `FAIL=0`
- `WARN=0`

结论：

- 旧口径硬风险清理后，当前系统处于绿色状态。

---

## 六、当前可以说什么

可以说：

- 本轮旧口径硬风险清理完成。
- 38789 不再作为当前 Core 端口定义。
- 60789 仍为旧测试口，不作为正式依据。
- qwen 仍为历史旧口径，不进 PG 主链。
- 当前搜索链仍为 router-search-v1 / brain-search-v3 / memory-search-v1。
- safety-check 绿色。

---

## 七、当前不能说什么

不能说：

- 02 / PG 正式区全量精简完成。
- 解释性规则全部压缩完成。
- 系统产品级稳定认证完成。
- 后续没有尾巴。

---

## 八、后续尾巴

仍未完成：

1. 解释性规则尚未全面压缩。
2. 相关解释卡尚未全部按新增验收题回压。
3. 02 正式区没有做大规模压缩。
4. PG 正式区没有做大规模压缩。
5. 系统产品级稳定性没有完成认证。
