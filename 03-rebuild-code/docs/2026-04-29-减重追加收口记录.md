# 2026-04-29 减重追加收口记录

## 一、本轮追加收口定位

本轮追加收口发生在 `trim-complete-2026-04-29` 标签之后。

本轮不是继续大规模删文件，而是补齐此前遗漏的尾巴：

1. 02 非入口目录边界。
2. PG 非入口目录边界。
3. 解释规则与验收项映射。
4. 最终七件套收口验收卡。
5. 最终 safety-check。
6. PG / Core 最小验收与安全体检口径补验。

---

## 二、已完成

### 1. 02 非入口目录说明

新增：

- `02-brain/00-GOVERNANCE/02-非入口目录说明.md`

作用：

- 明确 `07-RETIRING`、`08-IMPORT-STAGING`、`99-DEFERRED` 不是当前 02 正式入口。
- 防止旧 `CURRENT.md`、`README.md`、旧阶段材料被误当当前主脑入口。

对应提交：

- `81bd37d 减重：新增02非入口目录说明`

---

### 2. PG 非入口目录说明

新增：

- `PG/brain/00-GOVERNANCE/PG-非入口目录说明.md`

作用：

- 明确 PG 的 `07-RETIRING`、`08-IMPORT-STAGING`、`99-DEFERRED` 不是当前正式入口。
- 防止退役区旧身份、旧规则、旧端口材料反向污染正式 PG 脑。
- 特别排除旧 `38789` 口径。

对应提交：

- `b750636 减重：新增PG非入口目录说明`

---

### 3. 解释规则验收映射表

新增：

- `03-rebuild-code/docs/00-解释规则验收映射表.md`

作用：

- 把解释性规则对应到已有验收题、验收卡和 Skill。
- 防止规则越写越多但没有验收。
- 防止把“局部完成”说成“全部完成”。

对应提交：

- `c552620 减重：新增解释规则验收映射表`

---

### 4. 最终七件套收口验收卡

新增：

- `03-rebuild-code/docs/00-最终七件套收口验收卡.md`

七件套包括：

1. 当前主脑在哪里。
2. 当前运行体在哪里。
3. 当前入口读什么。
4. 当前健康检查怎么跑。
5. 不合格怎么回滚。
6. 哪些内容允许写回母本。
7. 哪些内容必须扔掉 / 降权。

对应提交：

- `de357e7 减重：新增最终七件套收口验收卡`

---

## 三、最终验收

### 1. safety-check

最终执行 `safety-check-v1.sh`，结果：

- Core `12289` 正在监听
- PG `58789` 正在监听
- LaunchAgent 全部加载
- 搜索体系检查通过
- 搜索路由冒烟测试通过
- System-Snapshots 当前分支为 `main`
- 工作区干净
- `FAIL=0`
- `WARN=0`

结论：

- 当前可以说：安全体检绿色。

---

### 2. PG / Core 最小验收 V2

PG 与 Core 均已确认：

- Core = `12289`
- PG = `58789`
- `60789` = 旧测试口，不作为正式依据
- `07 / 08 / 99` 是目录分区，不是端口，也不是正式入口
- PG 不是 Core 附属
- Gemma4 / oMLX 是外部能力工具，不是主脑，不进主模型槽
- 本轮追加收口完成 ≠ 全部系统性精简完成

---

### 3. 安全体检口径补验

PG 与 Core 均已确认：

- 可以说“安全体检绿色”
- 但不能夸大为：
  - 全部系统性精简完成
  - 02 / PG 脑已全量精简完成
  - 所有规则已压缩完成
  - 系统产品级稳定

---

## 四、仍未完成

以下内容仍未完成，后续不能遗漏：

1. 前层降权固定验收题尚未补。
2. PG 连续性固定验收题尚未补。
3. 解释性规则尚未全面压缩。
4. 02 正式区没有做大规模压缩。
5. PG 正式区没有做大规模压缩。
6. 全系统产品级稳定性没有完成认证。

---

## 五、当前结论

本轮追加减重与护栏建设可以收口。

可以说：

- 安全体检绿色。
- 本轮追加收口完成。
- 02 / PG 非入口护栏已补。
- 解释规则到验收项的映射已补。
- 最终七件套收口验收卡已补。
- PG / Core 最小验收与安全体检口径补验已通过。

不能说：

- 全部系统性精简完成。
- 所有规则都已压缩完成。
- 02 / PG 脑已经全量精简完成。
- 系统产品级稳定。

---

## 六、下一步优先项

下一轮优先处理：

1. 前层降权固定验收题。
2. PG 连续性固定验收题。

处理原则：

- 先补验收题。
- 再考虑压缩解释性规则。
- 不直接动 03 上位总卡。
- 不直接压缩 PG / 02 身份入口层。
