# 2026-04-29 解释卡回压第一批收口记录

## 一、本轮定位

本轮发生在 `trim-validation-cards-complete-2026-04-29` 之后。

目的不是全面压缩所有解释性规则，而是按新增验收题与规则入口体系，对第一批高价值候选卡进行回压、降权和短卡化。

本轮原则：

1. 不新增解释卡。
2. 不动 03 上位总卡。
3. 不动 02 / PG 身份入口层。
4. 只处理低风险局部卡、历史卡、旧工具说明卡。
5. 每批处理后必须跑 safety-check。

---

## 二、本轮已处理卡片

### 1. Core 密钥与前层降权修复记录

文件：

- `03-rebuild-code/docs/03-Core密钥与前层降权修复记录-V1.md`

处理结果：

- 从当前规则入口降权为历史修复记录。
- 去掉“当前仍需后续继续验收”的旧尾巴。
- 明确不得从本卡反向定义当前状态、当前尾巴、当前端口或当前验收结论。
- 当前状态改为以最新正式入口和验收卡为准。

提交：

- `52a8b4e 减重：降权Core密钥与前层降权修复记录`

---

### 2. 前层经历池抽取与入脑卡

文件：

- `03-rebuild-code/docs/03-前层经历池抽取与入脑卡.md`

处理结果：

- 从长解释卡压缩为 03 工艺短方法卡。
- 保留壳内材料进入母脑前的准入流程：
  - 前层经历池
  - `08-IMPORT-STAGING`
  - 分类判断
  - 分流
  - 升格 / 留存 / 延后
- 保留分类、分流、升格、禁止事项与验收标准。
- 明确前层经历池不是主脑。

提交：

- `fb6579e 减重：压缩前层经历池抽取与入脑卡`

---

### 3. PG 主脑检索工具旧说明

文件：

- `03-rebuild-code/docs/03-PG主脑检索工具-V1.md`

处理结果：

- 从 PG 检索工具说明压缩为旧工具说明。
- 明确本卡不再作为主脑搜索总规则。
- 明确当前搜索总规则以 `03-主脑搜索V2设计卡.md` 与 `00-规则权威入口表.md` 为准。
- 保留旧调用方式与边界。

提交：

- `aec7db4 减重：压缩PG主脑检索工具旧说明`

---

### 4. 主脑搜索 V2 设计卡

文件：

- `03-rebuild-code/docs/03-主脑搜索V2设计卡.md`

处理结果：

- 校准当前正式搜索工具链。
- 从旧 `brain-search.sh` 口径，更新为当前正式链：
  - `router-search-v1.sh`
  - `brain-search-v3.sh`
  - `memory-search-v1.sh`
- 明确 OpenClaw 原生搜索只作为未来增强层评估，不能取代主权搜索底盘。
- 将 safety-check 搜索路由冒烟测试纳入验收标准。

提交：

- `4e60795 减重：更新主脑搜索V2当前工具链`

---

## 三、验收结果

执行：

- `03-rebuild-code/tools/safety-check-v1.sh`

结果：

- Core `12289` 正在监听
- PG `58789` 正在监听
- LaunchAgent 全部加载
- 搜索体系检查通过
- 搜索路由冒烟测试通过
- System-Snapshots 当前分支为 `main`
- 工作区干净
- `FAIL=0`
- `WARN=0`

结论：

- 解释卡回压第一批后，当前系统处于绿色状态。

---

## 四、当前可以说什么

可以说：

- 解释卡回压第一批完成。
- 第一批处理了 4 张卡。
- 第一批后 safety-check 通过。
- 当前搜索工具链口径已校准为：
  - `router-search-v1.sh`
  - `brain-search-v3.sh`
  - `memory-search-v1.sh`

---

## 五、当前不能说什么

不能说：

- 解释性规则已经全面压缩完成。
- 02 / PG 正式区已经全量精简完成。
- 系统产品级稳定。
- 后续已经没有尾巴。
- 所有历史卡都已处理完。

---

## 六、后续尾巴

仍未完成：

1. 解释性规则尚未全面压缩。
2. 相关解释卡尚未全部按新增验收题回压。
3. 02 正式区没有做大规模压缩。
4. PG 正式区没有做大规模压缩。
5. 系统产品级稳定性没有完成认证。

---

## 七、下一步建议

下一轮如继续处理，建议只读扫描，不直接改：

1. `03-rebuild-code/docs/03-前台承诺抽取参考-V1.md`
2. `PG/brain/00-GOVERNANCE/CONTINUITY/PG-模型切换接班规则.md`
3. `PG/brain/00-GOVERNANCE/CONTINUITY/PG-模型槽与Gemma4调用规则-V1.md`

处理原则：

- 方法卡不轻易退役。
- 身份入口不动。
- PG 连续性规则先只读。
- 每批最多 2 到 4 张。
- 每批后必须跑 safety-check。
