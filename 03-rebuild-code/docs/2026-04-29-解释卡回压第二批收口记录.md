# 2026-04-29 解释卡回压第二批收口记录

## 一、本轮定位

本轮发生在 `trim-explain-cards-batch1-complete-2026-04-29` 之后。

目的不是继续大规模压缩，而是对第二批候选卡做只读扫描与小范围回压。

本轮原则：

1. 不为了压缩而压缩。
2. 方法卡不轻易退役。
3. PG 连续性规则先保稳定。
4. 只处理明显重复职责的卡。
5. 每批处理后必须跑 safety-check。

---

## 二、本轮扫描对象

本轮只读扫描了三张卡：

1. `03-rebuild-code/docs/03-前台承诺抽取参考-V1.md`
2. `PG/brain/00-GOVERNANCE/CONTINUITY/PG-模型切换接班规则.md`
3. `PG/brain/00-GOVERNANCE/CONTINUITY/PG-模型槽与Gemma4调用规则-V1.md`

---

## 三、本轮实际处理

### PG 模型切换接班规则

文件：

- `PG/brain/00-GOVERNANCE/CONTINUITY/PG-模型切换接班规则.md`

处理结果：

- 压缩模型切换接班规则。
- 保留“切换前交接、切换后确认、未接住必须明说”的连续性骨架。
- 加强切换后最小确认项：
  - 我是谁
  - 老林是谁
  - 当前监管主线程
  - 当前停点
  - 下一步核查
  - 当前不能夸大
  - 依据入口
- 删除重复模型槽位定义。
- 明确本卡只负责“PG 模型切换后的连续性接班”。
- 模型槽位、Gemma4 权限、qwen / GPT / Kimi 的位置，回到：
  - `PG-模型槽与Gemma4调用规则-V1.md`
  - `03-Core-PG主判与外部工具权力结构-V1.md`

提交：

- `cbc08d7 减重：压缩PG模型切换接班规则`

---

## 四、本轮确认不动

### 1. 前台承诺抽取参考

文件：

- `03-rebuild-code/docs/03-前台承诺抽取参考-V1.md`

判断：

- 不压缩。
- 不退役。
- 保留为 03 工艺方法卡。

原因：

- 本卡解决的是“有缺陷壳不能整包继承，但前台承诺与信任连续性不能粗暴丢弃”的问题。
- 这不是普通重复解释卡。
- 当前没有更合适的验收卡完全覆盖它。

### 2. PG 模型槽与 Gemma4 调用规则

文件：

- `PG/brain/00-GOVERNANCE/CONTINUITY/PG-模型槽与Gemma4调用规则-V1.md`

判断：

- 不压缩。
- 不退役。
- 保留为 PG 模型槽 / Gemma4 权限边界卡。

原因：

- 结构清楚。
- 行数不大。
- 已成为模型槽位、Gemma4 权限、qwen / GPT / Kimi 边界的承接卡。
- 若继续压缩，容易削弱模型边界。

---

## 五、验收结果

执行：

- `03-rebuild-code/tools/safety-check-v1.sh`

结果：

- Core `12289` 正在监听
- PG `58789` 正在监听
- LaunchAgent 全部加载
- 搜索体系检查通过
- 搜索路由冒烟测试通过
- System-Snapshots 当前分支为 `main`
- 工作区干净
- `FAIL=0`
- `WARN=0`

结论：

- 解释卡回压第二批后，当前系统处于绿色状态。

---

## 六、当前可以说什么

可以说：

- 解释卡回压第二批完成。
- 第二批实际处理 1 张卡。
- 第二批确认 2 张卡暂不处理。
- 第二批后 safety-check 通过。

---

## 七、当前不能说什么

不能说：

- 解释性规则已经全面压缩完成。
- 02 / PG 正式区已经全量精简完成。
- 系统产品级稳定。
- 后续已经没有尾巴。
- 所有 PG 连续性规则都已完成压缩。

---

## 八、后续尾巴

仍未完成：

1. 解释性规则尚未全面压缩。
2. 相关解释卡尚未全部按新增验收题回压。
3. 02 正式区没有做大规模压缩。
4. PG 正式区没有做大规模压缩。
5. 系统产品级稳定性没有完成认证。

---

## 九、下一步建议

下一轮如继续处理，建议先暂停大规模压缩，改为只读评估：

1. 02 正式区是否还有明显旧口径。
2. PG 正式区是否还有明显重复解释。
3. 是否需要建立“产品级稳定性观察清单”。

处理原则：

- 不动身份入口。
- 不动上位总卡。
- 每批最多 1 到 3 张。
- 每批后必须跑 safety-check。
