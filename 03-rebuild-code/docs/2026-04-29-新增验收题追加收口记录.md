# 2026-04-29 新增验收题追加收口记录

## 一、本轮定位

本轮发生在 `trim-additional-complete-2026-04-29` 标签之后。

目的不是继续大规模压缩规则，而是补齐两个此前明确留下的验收缺口：

1. 前层降权固定验收题。
2. PG 连续性固定验收题。

这两项是为了防止：

- 把前层印象冒充后层主脑依据。
- 把“我记得”冒充正式入口。
- 把局部闭环说成全部完成。
- PG 模型切换后只会复述碎片，接不住当前监管主线。

---

## 二、已完成

### 1. 前层降权固定验收题

新增：

- `03-rebuild-code/docs/00-前层降权固定验收题.md`

覆盖 5 个固定验收问题：

1. 前层是什么？为什么不是主脑？
2. 哪些问题必须进入后层正式入口？
3. 前层与后层冲突时听谁？
4. 什么叫读够即停？
5. 为什么不能用“我记得”代替正式入口？

对应提交：

- `30408a9 减重：新增前层降权固定验收题`

---

### 2. PG 连续性固定验收题

新增：

- `03-rebuild-code/docs/00-PG连续性固定验收题.md`

覆盖 5 个固定验收问题：

1. 当前监管主线程是什么？
2. 当前停点是什么？
3. 下一步核查什么？
4. 模型切换后如何确认真的接住？
5. 什么情况不算 PG 连续性通过？

对应提交：

- `3fc980f 减重：新增PG连续性固定验收题`

---

### 3. 验收映射与七件套状态更新

更新：

- `03-rebuild-code/docs/00-解释规则验收映射表.md`
- `03-rebuild-code/docs/00-最终七件套收口验收卡.md`

作用：

- 把“前层降权固定题 / PG 连续性固定题 / 七件套”从“待补”改为“已补”。
- 把下一步改为“最终 safety-check 与新增验收题最小复验”。
- 防止 PG / Core 继续读取旧状态，误判为“尚未补”。

对应提交：

- `bccbd3d 减重：更新验收映射与七件套状态`

---

## 三、验收结果

### 1. 新增验收题最小复验 V2b

PG / Core 均已确认：

- Core = `12289`
- PG = `58789`
- 前层降权固定验收题已补
- PG 连续性固定验收题已补
- 安全体检绿色 ≠ 全部系统性精简完成
- 本轮新增验收题完成 ≠ 所有解释性规则已全面压缩

结论：

- 新增验收题最小复验 V2b 通过。

---

### 2. bccbd3d 后最终 safety-check

最终执行 `safety-check-v1.sh`，结果：

- Core `12289` 正在监听
- PG `58789` 正在监听
- LaunchAgent 全部加载
- 搜索体系检查通过
- 搜索路由冒烟测试通过
- System-Snapshots 当前分支为 `main`
- 工作区干净
- `FAIL=0`
- `WARN=0`

结论：

- 当前系统处于绿色状态。

---

## 四、仍未完成

以下内容仍未完成，后续不能遗漏：

1. 解释性规则尚未全面压缩。
2. 相关解释卡尚未按新增验收题回压。
3. 02 正式区没有做大规模压缩。
4. PG 正式区没有做大规模压缩。
5. 系统产品级稳定性没有完成认证。

---

## 五、当前可以说什么

可以说：

- 新增前层降权固定验收题已完成。
- 新增 PG 连续性固定验收题已完成。
- 验收映射与七件套状态已更新。
- 新增验收题最小复验 V2b 通过。
- 最终 safety-check 通过。
- 本轮新增验收题追加收口完成。

---

## 六、当前不能说什么

不能说：

- 全部系统性精简完成。
- 所有解释性规则已经全面压缩。
- 02 / PG 脑已经全量精简完成。
- 系统产品级稳定。
- 后续已经没有尾巴。

---

## 七、下一步优先项

下一轮优先处理：

1. 按新增验收题，回压和压缩前层降权相关解释卡。
2. 按新增验收题，回压和压缩 PG 连续性 / 交接 / 模型切换相关解释卡。

处理原则：

- 先只读扫描。
- 不直接动 03 上位总卡。
- 不直接压缩 PG / 02 身份入口层。
- 每压缩一处，必须跑对应验收题。
